apiVersion: v1
kind: Secret
metadata:
  name: managify-secrets
  namespace: managify
type: Opaque
stringData:
  # You can fill these values directly (stringData will be converted to base64) or create the secret via kubectl create secret
  mongo-root-username: "REPLACE_USERNAME"
  mongo-root-password: "REPLACE_PASSWORD"

---

# Example imagePullSecret (only needed if your Docker Hub repo is private)
---
# Example imagePullSecret (only needed if your Docker Hub repo is private)
# Create with kubectl (safer) instead of storing credentials in git:
# kubectl create secret docker-registry regcred \
#   --docker-server=https://index.docker.io/v1/ \
#   --docker-username=${DOCKERHUB_USERNAME} \
#   --docker-password=${DOCKERHUB_TOKEN} \
#   --docker-email=you@example.com -n managify
